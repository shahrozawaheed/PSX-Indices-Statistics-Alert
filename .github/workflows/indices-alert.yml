name: PSX Indices Statistics Alert

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  indices-alert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Run indices alert
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python indices_alert.py

      - name: Save duplicate prevention data
        run: |
          if [ -f sent_indices_alerts.json ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

            git add sent_indices_alerts.json

            if git diff --cached --quiet; then
              echo "No new sent alerts to save."
            else
              git commit -m "Update sent index alerts"
              git push
            fi
          else
            echo "sent_indices_alerts.json does not exist because no new alert was sent."
          fi
